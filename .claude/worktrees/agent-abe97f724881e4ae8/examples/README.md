# Examples

This directory contains reference implementations that are not part of the
default CEO Agent installation or active C-suite roster.

`dispute-agent/` demonstrates a domain-specific bridge with task allowlisting,
runtime configuration through environment variables, and safe webhook-secret
handling. Installers may adapt it for their own use case after reviewing its
domain assumptions and security requirements.
